permissions:
  contents: read
name: Docs

on:
  push:
    branches: [main]
  pull_request:

jobs:
  docs:
    name: Docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      
      # Install Nix with Determinate Systems (faster and more reliable)
      - uses: DeterminateSystems/determinate-nix-action@v3
      
      # Enable zero-configuration binary cache for Nix
      - uses: DeterminateSystems/flakehub-cache-action@main
      
      # Check flake.lock health
      - uses: DeterminateSystems/flake-checker-action@main
      
      # Simplify by setting up environment once and running all commands in it
      - name: Setup and build docs
        run: |
          nix develop -c bash -c '
            # Create and activate virtual environment
            uv venv
            source .venv/bin/activate
            
            # Install dependencies
            uv pip install -e ".[dev]"
            
            # Build documentation
            if [ -d docs/_build ]; then rm -rf docs/_build; fi
            sphinx-build docs docs/_build
          '
      - name: Upload docs
        uses: actions/upload-artifact@v4
        with:
          name: docs
          path: docs/_build
